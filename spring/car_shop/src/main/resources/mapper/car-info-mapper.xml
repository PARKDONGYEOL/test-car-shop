<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.car_shop.car_info.mapper.CarInfoMapper">
    <resultMap id="carInfo" type="com.green.car_shop.car_info.dto.CarInfoDTO">
        <id     column="MODEL_NUM"      property="modelNum"/>
        <result column="MODEL_NAME"     property="modelName"/>
        <result column="MAKER_NAME"     property="makerName"/>
        <result column="CAR_PRICE"      property="carPrice"/>
    </resultMap>

    <insert id="postCarInfo">
        INSERT INTO car_info (
            MODEL_NAME,
            MAKER_NAME,
            CAR_PRICE
        ) VALUES (
            #{modelName},
            #{makerName},
            #{carPrice}
        )
    </insert>

    <select id="getCarInfoList" resultMap="carInfo">
        SELECT  MODEL_NUM,
                MODEL_NAME,
                MAKER_NAME,
                CAR_PRICE
        FROM car_info
        ORDER BY MODEL_NUM DESC
    </select>

    <select id="getSelectModel" resultType="String">
        SELECT CONCAT(MODEL_NUM, ':', MODEL_NAME, '[', MAKER_NAME, ']') AS NAME
        FROM car_info
    </select>

</mapper>


























